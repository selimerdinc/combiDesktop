version: '3.8'

services:
  kombi:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - kombi-db:/app/data
    restart: unless-stopped
    environment:
      - TZ=Europe/Istanbul

volumes:
  kombi-db:

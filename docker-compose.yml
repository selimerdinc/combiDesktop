version: '3.8'

services:
  kombi:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - kombi-data:/app
    restart: unless-stopped
    environment:
      - TZ=Europe/Istanbul

volumes:
  kombi-data:
